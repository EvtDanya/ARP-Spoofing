# ARP spoofing

# How to start   
• clone repository: 
  ```
  git clone https://github.com/EvtDanya/ARP-Spoofing
  ``` 
• go to install folder and install requirements:  
  ```
  chmod +x install.sh
  sudo ./install.sh
  ``` 

# About  
In work